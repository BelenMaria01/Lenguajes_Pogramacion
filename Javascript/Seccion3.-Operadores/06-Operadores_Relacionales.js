let a = 3,
  b = 2,
  c = "3";

let resu = a < b; // menor que
console.log(resu);

resu = a <= b; //menor o igual que
console.log(resu);

resu = a > b; // mayor que
console.log(resu);

resu = a >= b; // mayor o igual que
console.log(resu);
