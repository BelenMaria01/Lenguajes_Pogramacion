// * Si tiene dos palabra (nombre completo), la segunda letra de la segunda palabra
// * tiene que ser en mayuscula (nombreCompleto)
let nombreCompleto = "Juan juanito juan";
console.log(nombreCompleto);

// * Se puede declarar varias variables en una linea y le podemos asignar
// * variables en una sola linea.
let x, y;
(x = 10), (y = 20);
let z = (x+y);
console.log(z);
 